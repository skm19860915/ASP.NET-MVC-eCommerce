@{
    ViewBag.Title = "Success";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}
@using Platini.Models
@model Guid

@if (Model != null && Model != Guid.Empty)
{
    @Html.Action("Receipt", "Home", new { Id = Model })
}

